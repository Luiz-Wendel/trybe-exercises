const ping = require('./ping');
const upload = require('./upload');
const multiple = require('./multiple');
const profilePics = require('./profilePics');

module.exports = {
  ping,
  upload,
  multiple,
  profilePics,
};
